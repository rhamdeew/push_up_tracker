# Push Up Tracker Configuration

# Server port (default: 8080)
PORT=8080

# Basic authentication credentials
USERNAME=admin
PASSWORD=admin

# Working directory (automatically set during installation)
PWD=/opt/push_up_tracker